export const CartWidget = ({ cart }) => {
    return (
        <div>  
            <img src='components\CartWidget\CartWidget.jpg'>                
            </img>    
            <p>{cart.length}</p>
        </div>

    )}